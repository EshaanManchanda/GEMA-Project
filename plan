
● User approved Claude's plan
  ⎿  Plan saved to: C:\Users\eshaa\.claude\plans\swift-mapping-truffle.md · /plan to edit
     Plan: Enable Full HTML Support in TipTap Editor

     Context

     User wants the Code2 button (HTML insertion) to actually support the pottery workshop HTML documented in TIPTAP_USAGE_GUIDE.md.

     Current Status: ❌ POTTERY HTML WILL NOT WORK

     Critical Issues Found:

     1. TipTap Schema Restrictions - Strips semantic tags (<main>, <aside>, <nav>)
     2. No Iframe Support - Only YouTube iframes work, Instagram will be stripped
     3. No Style Attribute Parsing - All inline styles removed (flexbox, grid, sticky positioning)
     4. Strict Document Structure - Complex nested divs flattened/removed

     Investigation Results

     Files Analyzed:

     1. E:\coding\gema\frontend\src\components\common\HtmlInsertModal.tsx (lines 16-31)
       - Passes raw HTML without sanitization
     2. E:\coding\gema\frontend\src\components\common\TipTapEditor.tsx
       - Lines 68-116: Extension configuration (StarterKit only)
       - Lines 195-204: handleInsertHtml uses editor.chain().focus().insertContent(html).run()
       - Problem: TipTap schema strips unsupported tags/attributes
     3. E:\coding\gema\frontend\src\pages\static\BlogDetailPage.tsx (lines 410-419)
       - DOMPurify allows iframes and style attributes
       - But: HTML must survive TipTap parsing first

     What Currently Works:

     - Basic formatting (bold, italic, underline)
     - Headings (H1, H2, H3)
     - Lists, links, images
     - YouTube embeds
     - Blockquotes, code blocks

     What Doesn't Work (Pottery Workshop HTML):

     - ❌ Instagram iframes
     - ❌ Semantic tags (<main>, <aside>, <nav>)
     - ❌ Inline flexbox styles
     - ❌ CSS Grid styles
     - ❌ Sticky positioning
     - ❌ Complex nested div structures

     Solution Approaches

     Option 1: Add Custom TipTap Extensions (Complex)

     Pros: Maintains TipTap editor integration
     Cons: Requires 4-5 custom extensions, complex schema definitions

     Required Extensions:
     1. Generic Iframe extension (Instagram support)
     2. Semantic tags extension (main, aside, nav, section, article)
     3. Div container extension (nested divs with classes)
     4. Style attribute extension (preserve inline styles)

     Files to modify:
     - Create: frontend/src/components/common/tiptap-extensions/IframeExtension.ts
     - Create: frontend/src/components/common/tiptap-extensions/SemanticTagsExtension.ts
     - Create: frontend/src/components/common/tiptap-extensions/DivExtension.ts
     - Create: frontend/src/components/common/tiptap-extensions/StyleAttributeExtension.ts
     - Modify: frontend/src/components/common/TipTapEditor.tsx (add extensions to config)

     Complexity: HIGH (100-200 lines per extension)

     ---
     Option 2: Raw HTML Mode (Recommended) ⭐

     Pros: Simple, preserves ALL HTML, no parsing issues
     Cons: Bypasses TipTap for complex HTML (but still allows simple editing)

     Approach:
     Store two versions of content:
     1. Rich content - TipTap parsed (for toolbar editing)
     2. Raw HTML content - Unmodified (for complex layouts)

     When user inserts complex HTML via Code2 button:
     - Detect if HTML contains unsupported features (iframes, semantic tags, complex styles)
     - Store as raw HTML in separate field
     - Display warning: "Complex HTML detected - will bypass editor parsing"
     - Render raw HTML directly on blog page (DOMPurify only)

     Files to modify:
     1. Backend model (backend/src/models/blog.model.ts)
       - Add field: rawHtmlContent?: string
     2. Frontend TipTapEditor (frontend/src/components/common/TipTapEditor.tsx)
       - Modify handleInsertHtml to detect complex HTML
       - Show modal: "Use Raw HTML Mode?"
     3. Blog display page (frontend/src/pages/static/BlogDetailPage.tsx)
       - Check if rawHtmlContent exists, render that instead of content
     4. HtmlInsertModal (frontend/src/components/common/HtmlInsertModal.tsx)
       - Add detection logic for complex HTML
       - Show info box: "Detected: iframes, semantic tags, inline styles - will use Raw HTML mode"

     Complexity: LOW (50-80 lines total)

     ---
     Option 3: Hybrid Approach (Best of Both) 🏆

     Pros: Simple editing with toolbar + complex HTML support
     Cons: Slightly more complex state management

     How it works:
     1. User can edit with toolbar (TipTap) OR insert complex HTML (Code2 button)
     2. When complex HTML inserted:
       - Switch to "HTML Mode" - shows code editor
       - Disable toolbar (show message: "HTML Mode - toolbar disabled")
       - Store in rawHtmlContent field
     3. User can click "Switch to Editor Mode" to convert back to TipTap
       - Warning: "Complex HTML will be simplified"

     Files to modify:
     1. Backend model - Add contentMode: 'editor' | 'html' and rawHtmlContent
     2. TipTapEditor - Add mode toggle, HTML textarea view
     3. Blog form - Handle both modes
     4. Blog display - Render based on mode

     Complexity: MEDIUM (100-150 lines)

     ---
     Recommended Approach: Option 2 (Raw HTML Mode)

     Why This is Best:

     1. Preserves pottery HTML exactly - No parsing, no stripping
     2. Simple to implement - Minimal code changes
     3. No TipTap extension complexity - Avoids schema hell
     4. Security maintained - DOMPurify still sanitizes on render
     5. Backward compatible - Existing blogs unaffected

     Implementation Plan

     Step 1: Update Backend Model

     File: backend/src/models/blog.model.ts

     Add field after content:
     rawHtmlContent: {
       type: String,
       default: null,
       trim: true
     }

     Step 2: Detect Complex HTML

     File: frontend/src/components/common/TipTapEditor.tsx

     Update handleInsertHtml (lines 195-204):
     const handleInsertHtml = useCallback((html: string) => {
       if (!editor) return;

       // Detect complex HTML features
       const hasIframes = /<iframe/i.test(html);
       const hasSemanticTags = /<(main|aside|nav|article|section)/i.test(html);
       const hasComplexStyles = /style="[^"]*(?:display:\s*(?:flex|grid)|position:\s*sticky)/i.test(html);

       const isComplexHtml = hasIframes || hasSemanticTags || hasComplexStyles;

       if (isComplexHtml) {
         // Show modal: "Complex HTML detected - use Raw HTML mode?"
         const useRaw = window.confirm(
           'This HTML contains advanced features (iframes, layouts, semantic tags) that may not work in the editor.\n\n' +
           'Would you like to use Raw HTML mode? This will preserve all styling but disable toolbar editing for this content.'
         );

         if (useRaw) {
           // Emit event to parent to store in rawHtmlContent
           onChange(`__RAW_HTML__${html}`); // Special marker
           toast.success('Complex HTML will be preserved in Raw mode');
           return;
         }
       }

       // Normal TipTap insertion
       try {
         editor.chain().focus().insertContent(html).run();
       } catch (error) {
         console.error('Error inserting HTML:', error);
         toast.error('Failed to insert HTML content');
       }
     }, [editor, onChange]);

     Step 3: Handle Raw HTML in Blog Form

     File: frontend/src/pages/admin/BlogForm.tsx

     In form submission handler, check for __RAW_HTML__ marker:
     if (content.startsWith('__RAW_HTML__')) {
       blogData.rawHtmlContent = content.replace('__RAW_HTML__', '');
       blogData.content = '<p>This blog uses custom HTML layout.</p>'; // Placeholder
     } else {
       blogData.content = content;
       blogData.rawHtmlContent = null;
     }

     Step 4: Update Blog Display

     File: frontend/src/pages/static/BlogDetailPage.tsx

     Modify render logic (around line 410):
     <div
       className="blog-content"
       dangerouslySetInnerHTML={{
         __html: DOMPurify.sanitize(
           blog.rawHtmlContent || blog.content, // Use raw if available
           {
             ADD_ATTR: ['style', 'class'],
             ADD_TAGS: ['iframe'],
             ALLOWED_ATTR: ['style', 'class', 'href', 'src', 'alt', 'title',
                            'target', 'rel', 'width', 'height', 'id', 'scrolling']
           }
         )
       }}
     />

     Step 5: Update API/Controllers

     Files:
     - backend/src/controllers/blog.controller.ts
     - backend/src/validators/blog.validator.ts

     Add rawHtmlContent to allowed fields in validation and responses.

     Step 6: Update Documentation

     File: TIPTAP_USAGE_GUIDE.md

     Add note in "Inserting Custom HTML" section:
     **Note:** When inserting complex HTML (iframes, semantic tags, advanced layouts),
     the system will automatically use "Raw HTML Mode" to preserve all features.
     The toolbar will be disabled for this content, but your HTML will render exactly as designed.

     ---
     Alternative: Quick Fix (No Backend Changes)

     If you want to test immediately without backend changes:

     Use TipTap's setContent() Instead

     File: frontend/src/components/common/TipTapEditor.tsx

     Change line 199 from:
     editor.chain().focus().insertContent(html).run();

     To:
     editor.commands.setContent(html, false); // false = don't parse, keep raw HTML

     BUT: This is not fully supported and may cause issues. The schema will still reject unknown tags.

     ---
     Files to Modify (Option 2 - Recommended)

     1. Backend:
       - backend/src/models/blog.model.ts - Add rawHtmlContent field
       - backend/src/controllers/blog.controller.ts - Include field in responses
       - backend/src/validators/blog.validator.ts - Allow field in validation
     2. Frontend:
       - frontend/src/components/common/TipTapEditor.tsx - Detect complex HTML, add confirmation
       - frontend/src/pages/admin/BlogForm.tsx - Handle __RAW_HTML__ marker
       - frontend/src/pages/static/BlogDetailPage.tsx - Render raw HTML when available
       - TIPTAP_USAGE_GUIDE.md - Add Raw HTML mode documentation

     Estimated Changes

     - Backend: 3 files, ~20 lines
     - Frontend: 3 files, ~50 lines
     - Documentation: 1 file, ~15 lines
     - Total: ~85 lines

     Testing Plan

     1. Insert simple HTML (headings, paragraphs) - should use normal TipTap mode
     2. Insert pottery workshop HTML - should trigger Raw HTML mode confirmation
     3. Confirm - content saved in rawHtmlContent field
     4. View blog - pottery HTML renders perfectly with all styles, iframes, semantic tags
     5. Edit blog - shows Raw HTML mode indicator
     6. Switch back to editor mode (optional feature) - warning shown

     Security Considerations

     - DOMPurify still sanitizes on render (removes scripts, dangerous attributes)
     - No XSS risk - same security as current implementation
     - Instagram iframes allowed (already in DOMPurify config)
     - scrolling attribute added to ALLOWED_ATTR for iframe compatibility

     Post-Implementation

     Update TIPTAP_USAGE_GUIDE.md to reflect:
     - Complex HTML automatically detected
     - Raw HTML mode preserves all features
     - Simple editing still works with toolbar
     - Pottery workshop HTML works 100%

● Perfect! The plan is approved. Let me implement the "Raw HTML Mode" solution to make your pottery workshop HTML work perfectly. I'll start with the backend model update: