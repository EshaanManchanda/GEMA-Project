TASK: PLAN MODE

Review only the module's source files provided below (and the style reference above, for
conventions — do not plan tests for the style reference file itself). Produce a test PLAN only.
Do not write test code in this mode.

Output exactly these five sections, in this order, as plain text/markdown (no code fences
around the whole response):

1. Important routes/functions
   - List the HTTP routes (method + path) or exported functions this module exposes that are
     worth testing, with a one-line note on what each does.

2. Test cases
   - Group into "Success cases" and "Failure / security cases".
   - Be specific: name the exact request shape (fields, not vague "invalid input"), the
     expected status code, and what should be asserted in the response.
   - Include security-relevant cases where applicable: wrong role accessing a protected route,
     missing/invalid auth token, IDOR (accessing another user's resource by id), NoSQL
     injection attempts in query/body fields, oversized payloads, server-side trust boundaries
     (e.g. client-supplied price/amount/role fields that must be ignored server-side).

3. Required mocks
   - List exactly which of config/redis, config/firebase, config/stripe, services/email.service,
     services/queue.service (or others) this module's code path touches and therefore needs
     mocked, based on the actual imports you see in the source files.

4. Suggested test file names/paths
   - Propose file paths under `backend/src/tests/integration/`, following the existing
     subfolder pattern (e.g. `backend/src/tests/integration/<area>/<name>.test.ts`).

5. Risk areas
   - Call out anything in the source that looks fragile, under-validated, or worth a human's
     attention — but do not propose changing production code, just flag it.

Do not include any test code in this response. Do not include commentary outside these five
sections.
